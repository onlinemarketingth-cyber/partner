# Real production value — see frontend-admin/.env.production.example for
# why this file exists (Vite layers this on top of the committed
# frontend-admin/.env dev defaults for any `vite build`). Gitignored,
# not secret, just environment-specific.

VITE_API_BASE_URL=https://api.partner.syncvision.io
