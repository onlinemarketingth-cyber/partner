# Bug fix (2026-08-02) — was http://localhost:8010, same session-cookie
# collision issue described in frontend/.env's VITE_API_BASE_URL comment.
# Routed through this app's own hostname (admin.localhost) so its
# session cookie stays separate from the Agent Portal's.
VITE_API_BASE_URL=http://admin.localhost:8010
