<script setup lang="ts">
/**
 * DEPRECATED — moved to /frontend-admin/src/views/AdminHomeView.vue
 * per ADR-003 (Admin is now a separate Vue app, not a route in the
 * Agent Portal). This file is unreferenced by router/index.ts and only
 * left in place because the sandbox that authored it can't delete
 * files on this machine — safe to delete yourself:
 *   rm frontend/src/views/AdminView.vue
 */
</script>
